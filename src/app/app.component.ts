import { Component } from '@angular/core';

@Component({
  selector: 'app',
  template: `<div class="bg-success p-1 text-center">
  				To jest aplikacja SportsStore.
  			</div>`
})
export class AppComponent {}
